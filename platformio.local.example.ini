; Exemple de parametres propres a une machine. Ne pas versionner la copie reelle.
; Copier temporairement ces lignes dans platformio.ini pour un upload OTA,
; puis garder cette configuration hors de tout commit.
;
; [env:esp32dev]
; upload_protocol = espota
; upload_port = 192.0.2.1
;
; Le mot de passe OTA reste configure dans include/config.h.
